import React from 'react';

const NotFound = () => (
  <div className="main-content not-found">
    <h2>Sorry, something went wrong. The page you are looking for does not exist</h2>
  </div>
);

export default NotFound;